# ---- AI provider ----
AI_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# ---- Free tier ----
FREE_DAILY_LIMIT=3
LIVE_DAILY_LIMIT=2
RECRUITER_FREE_JOBS_DAILY=5
RECRUITER_FREE_RANK_RUNS_DAILY=5
RECRUITER_FREE_INTERVIEWS_DAILY=5
JOB_AGENT_FREE_RUNS_DAILY=5

# ---- Google sign-in ----
GOOGLE_CLIENT_ID=
AUTH_SESSION_SECRET=
RECRUITER_AUTH_SESSION_DAYS=30

# ---- Stripe billing (test/live switch) ----
STRIPE_MODE=test
STRIPE_TEST_SECRET_KEY=
STRIPE_TEST_PRICE_ID=
STRIPE_TEST_WEBHOOK_SECRET=
STRIPE_LIVE_SECRET_KEY=
STRIPE_LIVE_PRICE_ID=
STRIPE_LIVE_WEBHOOK_SECRET=

# ---- Postgres ----
# Existing app/member/recruiter database.
DATABASE_URL=
# Optional dedicated database for the job-search agent. If blank, DATABASE_URL is used.
SMARTJOBS_DATABASE_URL=
PGSSL=false

# ---- Job-search agent ----
APP_BASE_URL=https://your-smartjobs-domain.example
JOB_AGENT_SCHEDULER_ENABLED=true
JOB_AGENT_SCHEDULER_INTERVAL_MINUTES=15
JOB_AGENT_SCHEDULER_BATCH_SIZE=4
JOB_AGENT_MAX_QUERIES=36
JOB_AGENT_MAX_DISCOVERED=100
JOB_AGENT_VERIFY_CONCURRENCY=5
JOB_AGENT_SEARCH_TIMEOUT_MS=15000
# Search provider: auto, bing_rss, serper, or brave. Auto prefers a configured paid API and otherwise uses Bing RSS.
JOB_AGENT_SEARCH_PROVIDER=auto
JOB_AGENT_SEARCH_RSS_URL=https://www.bing.com/search
SERPER_API_KEY=
BRAVE_SEARCH_API_KEY=

# ---- SMTP nightly summaries ----
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_STARTTLS=true
SMTP_REJECT_UNAUTHORIZED=true
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=SmartJobs <jobs@yourdomain.com>
SMTP_FROM_ADDRESS=jobs@yourdomain.com
SMTP_HELO_NAME=smartjobs.yourdomain.com

# PORT is normally set by PM2/environment.
PORT=3000
